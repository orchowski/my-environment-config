# To use id do `source /full/path/to/this/file` in your's ~/.ideavimrc file
# and make things work
# 
#
# Project exploration
set NERDTree
set highlightedyank
let g:highlightedyank_highlight_duration = "-1"

# should match if else etc. use % and g% 
#   For example, go from "if" to "endif" to "else".
set matchit

map <c-w><c-w> :NERDTreeToggle<CR>

set ideajoin

set nu rnu

nnoremap <c-t> :action ActivateTerminalToolWindow<CR>
nnoremap <c-t><c-t> :action Terminal.OpenInTerminal<CR>

nnoremap [m :action MethodUp<CR>
nnoremap ]m :action MethodDown<CR>

nnoremap <C-i> :action Forward<CR>
nnoremap <C-o> :action Back<CR>

set clipboard+=unnamed
